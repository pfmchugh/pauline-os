{
  "name": "pauline-os",
  "version": "2.0.0",
  "description": "Pauline McHugh — retro desktop portfolio",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
